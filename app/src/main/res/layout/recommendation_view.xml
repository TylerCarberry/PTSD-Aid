<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/recommendation_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin">

    <ImageView
        android:id="@+id/bullet_imageview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/bullet_point"/>

    <TextView
        android:id="@+id/recommendation_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/bullet_imageview"
        android:layout_toRightOf="@id/bullet_imageview"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:text="Recommendations placeholder"
        android:fontFamily="sans-serif-light"
        android:textSize="27sp"/>

</RelativeLayout>