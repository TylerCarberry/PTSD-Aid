machine:
  environment:
    ADB_INSTALL_TIMEOUT: 10 # minutes (2 minutes by default)

test:
  override:
  
    # The two files containing API keys are not tracked in git
    # Write them from enviroment variables
    - echo $google_services_json3 > app/google-services.json
    - echo $ids_xml_3 > app/src/main/res/values/ids.xml
    
    # start the emulator
    - emulator -avd circleci-android22:
        background: true
        parallel: true
    # wait for it to have booted
    - circle-android wait-for-boot
    
    - sleep 60
    
    # Remove the lockscreen
    - adb shell input keyevent 82
    
    # run tests  against the emulator.
    - ./gradlew connectedAndroidTest
    
    # copy the build outputs to artifacts
    - cp -r PTSD/build/outputs $CIRCLE_ARTIFACTS
    # copy the test results to the test results directory.
    - cp -r PTSD/build/outputs/androidTest-results/* $CIRCLE_TEST_REPORTS